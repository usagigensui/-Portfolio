<%= form_with model: [profile, profile.color], url: colors_path(profile.code), local:true do |f| %>

  <p>複数項目で同じ色を選択すると、文字が見えなくなってしまう恐れがありますのでご注意ください。</p>

  <!--エラーメッセージ-->
  <%= render "layouts/error", model: f.object %>

  <div class="form-group row mt-3 mb-3">
    <%= f.label :text, "文字色", class: "col-form-label col-1" %>
    <%= f.color_field :text, class: "form-control col-3" %>
  </div>

  <div class="form-group row mb-3">
    <%= f.label :background, "背景色", class: "col-form-label col-1" %>
    <%= f.color_field :background, class: "form-control col-3" %>
  </div>

  <div class="form-group row mb-3">
    <%= f.label :accent, "差し色", class: "col-form-label col-1" %>
    <%= f.color_field :accent, class: "form-control col-3" %>
  </div>

  <div class="form-check mb-3">
    <%= f.check_box :default, {class: "form-check-input"}, true, false %>
    <%= f.label :default, "デフォルトカラーに戻す", class: "form-check-label" %>
  </div>

  <div class="form-group mb-3">
    <%= f.submit "確定", class: "btn btn-outline-success" %>
  </div>

<% end %>